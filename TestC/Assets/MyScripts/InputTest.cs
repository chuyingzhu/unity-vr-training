using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Valve.VR;


public class InputTest : MonoBehaviour {
    /* 
    public SteamVR_Input_Sources handType;
    public SteamVR_Action_Boolean teleportAction;
    public SteamVR_Action_Boolean grabAction;

    // Start is called before the first frame update
    void Start() {
        InputDetector myInput = new InputDetector();
    }

    // Update is called once per frame
    void Update() {
        myInput.SetHandType(handType);
        myInput.SetTeleportAction(teleportAction);
        myInput.SetGrabAction(grabAction);
        
        if (myIn.GetTeleportDown()) {
            print("Teleport " + myInput.handType);
        }

        if (myIn.GetGrab()) {
            print("Grab " + myInput.handType);
        }
    }
    */
}